- [ ] Add GSAP sidebar expand/collapse animation in src/components/DashBoard/Menu.jsx without changing existing JSX structure.
- [ ] Add GSAP animations for menu labels (fade/slide + stagger on expand; reverse on collapse).
- [ ] Add GSAP logo title/subtitle animation with slight delay.
- [ ] Add GSAP notification badge pop animation (only if badges exist in code).
- [ ] Add GSAP toggle arrow rotation + hide text animation (only if toggle exists in code).
- [ ] Add initial-load stagger (logo first, then menu items, then toggle).
- [ ] Ensure no Framer Motion usage remains and no layout shifts/flicker.
- [ ] Run lint/build to verify.

